# create namespace
window.ProfCyn = new Object()

ProfCyn.preloadCount = 10 # number of images to preload when reaching a certain scroll position, should be divisible by 2 since we're using its half for trigger calculations
